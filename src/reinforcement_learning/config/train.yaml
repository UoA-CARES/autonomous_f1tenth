train:
  ros__parameters:
    environment: 'CarTrackProgressiveGoal'  #'CarTrack' # CarGoal, CarWall, CarBlock, CarTrack, CarBeat
    track: 'multi_track' # track_1, track_2, track_3, multi_track, multi_track_testing -> only applies for CarTrack
    car_name: 'f1tenth'
    algorithm: 'PPO'
    g: 1
    buffer_size: 1000000
    max_steps_exploration: 1000
    observation_mode: 'lidar_only'
